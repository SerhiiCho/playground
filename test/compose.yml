name: test-project

services:
    app:
        build: .
        image: test-project
        container_name: test-project
        volumes:
            - .:/app:z
        tty: true
        environment:
            - PATH=/usr/local/go/bin:${PATH}
