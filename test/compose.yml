name: test-project

services:
    app:
        build:
            context: .
            dockerfile: Containerfile
        image: test-project
        container_name: test-project
        volumes:
            - .:/app:z
        tty: true
        stdin_open: true
